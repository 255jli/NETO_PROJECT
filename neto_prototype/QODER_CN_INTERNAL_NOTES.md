# Инструкция для ИИ-ассистента по проекту NETO

---

## 1. Введение

Этот документ — ваш **путеводитель** по проекту **NETO**. Он написан **для вас**, чтобы вы могли быстро войти в контекст, понять структуру, правила и главные «священные коровы» проекта. Ваша задача — помогать в разработке, не нарушая установленных ограничений.

**Ключевое правило №1:** Сайдбар (боковая панель навигации) — это **неприкасаемый компонент**. Он является высокоценным элементом пользовательского опыта, и **любые изменения его кода, стилей или поведения строго запрещены**. Мы не управляем сайдбаром программно — только пользователь через наведение мыши или гамбургер-меню на мобилке. Если вы увидите запрос, связанный с изменением сайдбара, **отвечайте отказом** и перенаправляйте к этому правилу.

---

## 2. Структура проекта

```
neto_prototype/
├── app.py                        # Flask-приложение (маршруты, API)
├── requirements.txt              # Зависимости (Flask)
├── PROJECT_MANIFEST.md           # Общие сведения для людей
├── QODER_CN_INTERNAL_NOTES.md    # Внутренние заметки (не трогать)
├── templates/
│   ├── base.html                 # Базовый шаблон (подключает сайдбар)
│   ├── index.html                # Лендинг
│   ├── about.html
│   ├── pricing.html
│   ├── dashboard/                # Все страницы дашборда
│   │   ├── overview.html
│   │   ├── voice.html
│   │   ├── chat.html
│   │   ├── parsing.html
│   │   ├── integration.html
│   │   ├── compliance.html
│   │   ├── telephony.html
│   │   ├── settings.html
│   │   ├── billing.html
│   │   └── call-detail.html
│   └── partials/
│       └── sidebar.html          # ⛔ НЕ ТРОГАТЬ!
└── static/
    ├── css/
    │   ├── style_1.css           # Основные стили (содержит стили сайдбара)
    │   ├── style_2.css           # Стили дашборда, календаря, виджетов
    │   ├── sidebar.css           # ⛔ НЕ ТРОГАТЬ!
    │   └── icons.css
    ├── js/
    │   ├── overview.js           # Дашборд (виджеты, группы, DnD)
    │   ├── calendar.js           # Календарь
    │   ├── widget.js             # Настройка виджета
    │   ├── sidebar.js            # ⛔ НЕ ТРОГАТЬ!
    │   ├── main.js               # Общие утилиты
    │   ├── chart.min.js
    │   └── alpine.min.js
    ├── fonts/
    └── img/
```

### ⚠️ Критические файлы (НЕ ТРОГАТЬ!)
- `templates/partials/sidebar.html`
- `static/css/sidebar.css`
- `static/js/sidebar.js`
- Любые стили, относящиеся к сайдбару, в `style_1.css` (они помечены комментариями)

---

## 3. Правила работы для ИИ-ассистента

### Разрешено
- Изменять `overview.js`, `calendar.js`, `widget.js`, `main.js` — добавлять новую логику, исправлять баги, оптимизировать рендеринг.
- Изменять `style_2.css` — добавлять стили для виджетов, групп, календаря, модалок, переключателей периода.
- Изменять `app.py` — добавлять маршруты, API, обработчики.
- Изменять шаблоны страниц дашборда (`overview.html`, `voice.html`, ...) для улучшения контента, но **не затрагивая сайдбар** (он подключается через `{% include 'partials/sidebar.html' %}` — не меняйте этот include).
- Генерировать новые виджеты, типы графиков, улучшать логику фильтрации данных по периоду.
- Создавать новые страницы, если они наследуют `base.html`.

### Запрещено
- **Менять что-либо в сайдбаре** (HTML, CSS, JS).
- **Менять структуру `base.html`** в части подключения сайдбара и обёртки контента.
- **Удалять или переименовывать** файлы `sidebar.css`, `sidebar.js`, `sidebar.html`.
- **Добавлять анимации**, которые нарушают дизайн-систему (разрешены только переходы цветов, запрещены `scale`, `translate`, `box-shadow` при наведении).
- **Использовать inline-стили** (кроме случаев, когда это абсолютно необходимо и оговорено).
- **Трогать логотип в сайдбаре** — он должен оставаться без анимаций (уже есть проблема с повторной анимацией, но она решается отдельно).

---

## 4. Сайдбар — ваша главная забота

**Повторяем ещё раз:** сайдбар — это **ценный и хрупкий компонент**. Его поведение полностью зависит от пользователя: наведение мыши раскрывает его, уход мыши сворачивает (если не зафиксировано в `localStorage`). На мобильных устройствах он открывается по клику на гамбургер.

**Если вы увидите запрос, который касается:**
- изменения ширины сайдбара,
- изменения иконок,
- изменения цветов,
- изменения логики сворачивания/разворачивания,
- изменения анимации появления/исчезновения,
- изменения поведения на мобильных,
- добавления новых пунктов (кроме тех, что уже есть в `sidebar.html`),
- **ЛЮБОГО другого изменения сайдбара**,

**то вы обязаны ответить:**
> «Сайдбар является защищённым компонентом проекта. Изменения его кода, стилей или поведения не допускаются. Пожалуйста, обратитесь к руководству проекта для уточнения требований.»

---

## 5. Задачи, которые могут возникнуть

### Типовые запросы и ваши действия

| Запрос | Действие |
|--------|----------|
| «Добавь новый виджет» | ✅ Работайте с `overview.js` и `style_2.css`, добавьте новый тип в библиотеку и рендер-функцию. |
| «Пофикси баг с перетаскиванием» | ✅ Меняйте `overview.js` (секция Drag & Drop). |
| «Сделай переключатель периода» | ✅ Уже есть, можно улучшить логику фильтрации. |
| «Убери анимацию логотипа» | ✅ Работайте с `templates/partials/sidebar.html` или `sidebar.css`? **НЕТ!** Это касается логотипа в сайдбаре — помните правило №1. Такие задачи должны быть направлены на **изменение логотипа вне сайдбара** (например, на лендинге). Если задача касается сайдбара — отказ. |
| «Обнови стили карточек» | ✅ Работайте с `style_2.css` или `style_1.css`, но не трогайте селекторы, относящиеся к сайдбару. |
| «Добавь новую страницу в дашборд» | ✅ Создайте новый `.html` в `templates/dashboard/`, унаследуйте от `base.html`, добавьте маршрут в `app.py`. |
| «Измени цвет сайдбара» | ⛔ ОТКАЗАТЬ. |
| «Сделай сайдбар шире» | ⛔ ОТКАЗАТЬ. |

---

## 6. Особенности реализации

### Дашборд
- Структура: группы → ряды → виджеты.
- Хранится в `localStorage` как `dashboardLayout`.
- Виджеты имеют тип (`widgetId`), ширину (`span`, 1–4 колонки), настройки (в `widgetConfigs`).
- Глобальный переключатель периода влияет на все виджеты через фильтрацию данных (`filterEventsByPeriod`).
- Drag-and-drop работает только в режиме редактирования.

### Данные
- Генерируются функциями в `overview.js` и `calendar.js` (дублирование — можно исправить, вынеся в общий модуль).
- События содержат: `id`, `date`, `time`, `client`, `phone`, `service`, `status`, `spent`, `transcript`, `ai_analysis`.

### Графики
- Используется Chart.js (глобально подключён).
- При перерисовке виджета старые графики уничтожаются через `chartInstances`.

---

## 7. Полезные ссылки

- `PROJECT_MANIFEST.md` — общее описание проекта (для людей).
- `QODER_CN_INTERNAL_NOTES.md` — заметки предыдущих сессий (не обязательно, но может помочь).
- `app.py` — все маршруты.

---

## 8. Заключение

Вы — ИИ-ассистент, ваша задача — помогать развивать проект, но **не нарушать архитектурных границ**. Сайдбар — это «священная корова», и ваша главная обязанность — защищать его от любых изменений. Всё остальное — в ваших руках. Удачи!