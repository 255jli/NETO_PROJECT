# NETO_PROJECT

**Важно**: Только фронтенд (без бэкенда). Все данные — статичные заглушки (data.js).
**CSS**: 
- `style_1.css` — основная вёрстка (≤800 строк)
- `style_2.css` — стили панели (≤200 строк)
- `style_3.css` — дополнительные стили (≤800 строк)
- `sidebar.css` — боковая панель

*Файлы разделены для удобства ИИ: один файл — не более 800 строк.*

**Для ИИ-ассистентов**:
1. Все правила — в `.codeassistant/`
2. Документация — в `MD/` (редактируйте файлы там)
3. Интеграция с бэкендом помечена `// API HOOK`